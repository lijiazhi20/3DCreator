<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Persist nothing sensitive; auth token lives in encrypted DataStore (handled in code). -->
    <include domain="sharedpref" path="." />
    <exclude domain="file" path="datastore/auth.pb" />
</full-backup-content>
