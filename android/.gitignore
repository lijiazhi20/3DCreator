# Compiled artifacts
/build/
*/build/
.gradle/
local.properties

# Gradle wrapper jar is generated by Android Studio / `gradle wrapper`
gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.DS_Store

# Signing
keystore.properties
release/
# Generated upload keystore + its secrets (NEVER commit — cloud build injects via secrets)
*.jks
release-key.properties
release-key.b64.txt
